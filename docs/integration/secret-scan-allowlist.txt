# Secret-scan findings that have been read and accepted.
#
# One line per accepted hit: `rule|path`, then a reason after a `#`. The pattern stays sharp;
# this is where a human says "I looked at that one".
#
# A scanner with no way to accept a finding is permanently red, and a permanently red gate is one
# everybody learns to ignore — which is worse than not having it at all. The alternative people
# reach for is loosening the pattern, and that blinds the scanner to the real thing too.
#
# Adding a line here is a visible diff on a file whose whole purpose is to be argued with. Do not
# add one without reading the blob.

assigned-secret|tests/Feature/Api/V1/CredentialLeakageTest.php    # The password posted by `a_password_never_reaches_the_log_or_the_response`. A fixture inside the test that exists to prove a password does not reach a log or a response.
dsn-with-password|tests/Feature/Console/ReadinessCommandTest.php  # The input and the expected output of `it_redacts_credentials_out_of_driver_errors`. The whole point of the test is that this string is redacted.
dsn-with-password|docs/integration/evidence-ledger.md             # A historical blob of this ledger, from before TAB 01 redacted the quotation. The value is the fake password from the test above. History is not rewritten to tidy a quotation.
assigned-secret|tools/local-api.sh                                # The fixed development password `tools/local-api.sh staff` sets on the seeded local account. It authenticates nobody anywhere else, the script prints it on purpose so it can be pasted into a local sign-in form, and the account only exists because DatabaseSeeder creates it when the environment is local or testing.
laravel-app-key|tests/Feature/Console/PreflightCommandTest.php    # `base64:S3CR3TAPPKEYVALUE...`, the fixture `no_credential_value_reaches_the_output` feeds the preflight reporter. The test asserts this exact string is absent from the command's output — a configuration reporter is precisely the tool that ends up printing a key into a deploy log. Reading as an app key is what makes the assertion meaningful.
aws-access-key-id|tests/Feature/Console/PreflightCommandTest.php  # `AKIAPRIVATEBUCKETKEY`, from the same test and the same reason. The string spells out what it is; no bucket anywhere holds it.
